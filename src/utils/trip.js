export default class Trip {
    constructor(id, title, purpose, project, task, locations) {
        this.id = id;
        this.title = title;
        this.purpose = purpose;
        this.project = project;
        this.task = task;
        this.locations = locations;
    }
}